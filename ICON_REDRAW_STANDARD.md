# FS 图标路径复刻规范

## 1. 唯一视觉来源

- 仅以 Iconfont 合集 `cid=26815` 与 `cid=54209` 的原始 SVG 为视觉母版。
- 不使用 Lucide、Material Symbols 或模型自由发挥的图形替换原图。
- 实心图标不进入本线性图标库。

## 2. 绘制坐标、Keyline 与视觉体量

### 2.1 基础坐标

- 以 `32 × 32` 为标准绘制网格，SVG 使用 `viewBox="0 0 32 32"`。
- 先把 Iconfont 原始轮廓等比映射到绘制网格，再从轮廓两侧的中点手工还原中心线。
- 路径坐标必须先忠实于来源。尺寸校准只能对整枚图标做等比缩放和轻微重心补偿，不能拉伸局部，也不能用放大掩盖路径缺失或拓扑错误。

### 2.2 四档中心线路径范围

Keyline 是复制到 MasterGo 后**路径中心线的几何外包范围**，不是包含描边后的可见范围，也不是对图标语义或外形的命名。圆形档位也可用于需要更大视觉体量的不规则图标。

| 外包档位 | 中心线路径尺寸 | 居中边界 | 典型用途 |
| --- | --- | --- | --- |
| 方形 | `24 × 24` | `x=4–28 / y=4–28` | 结构饱满的方框、矩形或常规不规则图标 |
| 大型/圆形 | `28 × 28` | `x=2–30 / y=2–30` | 圆形图标，以及线条稀疏、留白较大、视觉上容易显小的不规则图标 |
| 纵向 | `22 × 26` | `x=5–27 / y=3–29` | 主体明显纵向延伸且横向较窄的图标 |
| 横向 | `26 × 22` | `x=3–29 / y=5–27` | 主体明显横向延伸且纵向较矮的图标 |
| 纯箭头 | 左右 `10 × 18`；上下 `18 × 10` | 左右 `x=11–21 / y=7–25`；上下按方向旋转 | 独立分隔箭头与 `单箭头-*`；不用于带容器、横线或其他主体的复合图标 |
| 关闭 | `18.5 × 18.5` | `x=6.75–25.25 / y=6.75–25.25` | 独立关闭叉号 |
| 变大/变小 | `22 × 22` | `x=5–27 / y=5–27` | 由两组对角箭头组成的独立缩放图标 |

- 代码中的 `square / circle / vertical / horizontal` 只代表上述外包档位；不得仅凭“像圆、像方、像横向物体”机械选择。
- 描边宽度与路径尺寸相互独立。禁止从 Keyline 尺寸中减去描边宽度，也禁止因为描边变化而反向缩放中心线路径。
- 默认描边为 2，实际描边由全局参数控制，范围 0.5–10。例如方形图标在 MasterGo 中路径尺寸必须显示为 `24×24`，圆形图标必须显示为 `28×28`，描边仍单独显示为用户设置值。
- 上表尺寸以 `32×32` 图标画布为基准；选择其他图标尺寸时，路径 Keyline 按画布比例同步缩放。例如 `24px` 画布对应方形路径 `18×18`、圆形路径 `21×21`，描边仍保持用户输入的物理像素值。
- 对于非正方形轮廓，必须保持原始长宽比并完整放入对应档位，所以只有受限方向会达到档位最大值；不能为了同时填满宽高而拉伸路径。
- 纯箭头和关闭图标使用上述固定专用尺寸，不参与普通方形、横向、纵向或大型档位的视觉推断。左右箭头为 `10×18`；上下箭头为 `18×10`。代码中必须分别使用 `arrow` 与 `arrow-horizontal` 档位，不能依赖同一档位自动旋转。
- `变大`、`变小`使用固定 `22×22` 专用尺寸，不因箭头端点稀疏而升级到大型档位。

### 2.3 不规则图标的尺寸判断

四档外包范围只是初始候选，不能直接作为尺寸结论。不规则图标必须同时判断以下五项：

1. **有效主体**：主要语义结构实际占据的面积，不能把斜杠、箭头尖、后视镜、顶部横杆、圆点等极端节点当成主体尺寸。
2. **墨量与密度**：同样的外接框下，封闭结构、重复格子和多条平行线会显得更重；单圈轮廓、细长曲线和大留白会显得更轻。
3. **内部负空间**：外轮廓相同不代表视觉尺寸相同。大面积内腔、分件之间的空隙以及中心空洞都会降低视觉重量。
4. **长宽比与方向性**：横向或纵向档位只适用于有效主体本身明显呈长条形的图标，不能由一根伸出的附属线决定。
5. **同组视觉基线**：必须在同一 `32px / 2px 描边 / R1` 条件下，与当前批次中已确认的密集、稀疏、圆形和方形图标交叉比较，不能只和相邻一枚比较。

由此选择尺寸：

- 低密度、单圈轮廓、分叉多或内部留白大的图标，在 `24 × 24` 内仍显小时，可升级到 `28 × 28` 大型外包范围。
- 封闭结构多、重复单元多、线条密度高的图标，即使外形不满，也可能在 `28 × 28` 下显得过大，应回到 `24 × 24` 方形外包范围。
- 若完整几何包围盒很大，但有效主体仍小，应忽略极端附属节点后再判断；不能让一根长线同时决定缩放和视觉中心。
- 档位切换幅度过大时，允许在保持长宽比的前提下增加单图标光学校正，通常控制在 `0.92–1.08`；禁止拉伸局部或改变描边粗细来补偿尺寸。

### 2.4 外部尺寸与内部间距必须分开处理

“整体看起来偏大/偏小”和“图标内部太松/太挤”是两类问题，不能用同一种缩放解决：

- **外轮廓与主体同时偏小或偏大**：调整整枚图标的外包档位或光学校正比例。
- **外轮廓合适，但内部空隙过大**：这是构图比例问题，应按来源调整内侧端点、分件位置或内腔尺寸，不能放大整枚图标。
- **极端节点合适，但主体偏小**：先判断该节点是否为附属结构，再以有效主体为准重新缩放和居中。
- **主体大小合适，但视觉重心偏移**：只做轻微整体位移，不改变路径局部比例。

内部间距必须用当前描边宽度作为视觉标尺检查。在默认 `2px` 描边下，中心缝隙、箭头内端距离和分件间隔若成为第一视觉焦点，说明负空间过强；应先与原始 SVG 的比例核对，再收紧对应几何。外包尺寸合格不能替代这项检查。

### 2.5 多轮校准得到的具体经验

- `眼睛-不可见`：眼睛主体虽为横向，但遮挡斜杠使完整轮廓接近方形。使用横向档位会被高度限制而整体缩小，应使用大型外包范围。
- `交通-飞机`、`交通-公交`、`企业`、`路线`：这些图标由细长线段、分支或大面积内部留白组成，几何边界不小但视觉重量偏低，应使用大型外包范围与同组图标平衡。
- `路线`：原稿缺少左侧竖线到顶部弧线的连续段属于拓扑偏差，必须先补齐路径，再判断整体尺寸；不能单纯放大错误形状。
- `截图`：来源是两段连续裁切框加一根右上对角线，不是四个分离角标，也不是对角箭头；右上端不得增加箭头横边或竖边。叠加时若轮廓大致相似但连通关系不同，必须先恢复来源拓扑；禁止用常见图标库中的同名符号替代。
- `交通-公交`：后视镜属于来源结构，必须进入中心线路径；忽略附属结构后再放大车身，不算来源忠实复刻。
- `时间日期-秒表`、`定位`：两者虽然纵向明显，但主体都是单圈轮廓且内部留白较大，使用纵向档位后视觉重量不足；应按稀疏轮廓处理，使用大型外包范围。
- `银行`、`通用`：两者包含多条重复结构，墨量和节奏密度高；大型外包范围会比同组图标更重，应使用方形外包范围。不能因为轮廓接近最大边界就自动选择大型档位。
- `变小`：外轮廓尺寸正确时，中间两组箭头仍可能因内端距离过大而显得松散。这属于内部负空间问题，应收紧箭头内端，不能继续放大整枚图标。
- `上传`、`锁`：两者笔画不多，但外侧横线、箭头或锁梁会形成清晰而完整的外轮廓；完整 `28×28` 大型档位会让边界扩张感强于相邻图标。页面对比中 `0.92` 仍偏大、`0.80` 又偏小，最终应保留大型档位的形状逻辑并使用 `0.90` 的单图标光学校正。判断稀疏图标不能只看墨量，还要检查外轮廓是否已经形成强边界；当用户明确要求更大幅度校准时，可突破常规 `0.92–1.08` 范围并记录原因。
- `下午茶`、`删除`：在 Keyline 改为中心线几何尺寸后，完整 `28×28` 大型档位略显过重，但退回 `24×24` 方形档位又会偏小；应保留大型档位并使用约 `0.94` 的单图标光学校正，使主体落在两档之间。
- `对比`：整体轮廓接近方形，但右侧为断续线、内部留白大，完整几何边界并没有形成同等视觉重量；方形档位会明显偏小，应按稀疏结构使用大型档位。判断时不能只看长宽比，还要看边界是否连续闭合。
- `标签`、`发送`：内部虽然留白较大，但两者都有完整、尖锐且方向感强的闭合外轮廓；使用大型档位时外边界扩张感过强，应使用方形档位。稀疏或留白大不能单独触发大型档位，还要判断闭合轮廓是否已经形成强视觉边界。
- `附件`、`撤销`：连续曲线即使墨量不高，也会形成强而完整的运动轮廓；使用大型档位会比周围结构化图标更抢眼，应使用方形档位。曲线图标的尺寸判断要同时检查路径连续性与方向张力，不能机械归入稀疏大型档。
- `链接`：来源是沿左下至右上方向交扣的两个链环，整体横向与纵向跨度接近。不能把两个链环沿竖轴堆叠成近似 `S/8` 的窄长轮廓；必须保留约 45° 主轴、两个独立开口以及清楚的交扣关系。
- `语言-翻译`、`语言-英文`：字母不是装饰细节，而是区分图标语义的主体结构。重绘时必须把来源中的 `A`、`En` 连同外圈或边框完整还原；仅保留循环箭头、方框或单个 `E` 都属于拓扑缺失，不能通过尺寸校准掩盖。
- 边框内的字母、数字或符号必须按“内部内容组”的完整视觉外包范围居中，不能分别对齐单个字符。内部内容偏大或偏移时只调整内容组，不得连带缩放或移动已经正确的外框。
- 图标名称不能代替来源判断：例如本项目来源中的 `点赞` 是爱心、`公告` 是带顶部挂点的公告牌、`服务` 是带交扣结构的心形，不得套用常见图标库里的拇指、喇叭或耳机。
- 组织结构图中的方形节点、消息气泡中的圆点、用户画像的四个取景角等都是来源的辨识结构；几何类别和细节数量必须逐项还原，不能用圆形节点、空白气泡或普通用户轮廓替换。
- 证件、状态和消息类图标不得随意套用惯用元素：`护照`内部是人物头像而非地球，`VIP`内部只有来源的 V 形标记；`聊天记录`和`消息-已发送`的附加结构必须位于规定位置。
- 同一功能家族先检查来源是否确实共用基础轮廓，不得为了表面统一覆盖来源差异。本项目原始合集中的 `邮件-下载` 会在右侧和底部为下载箭头让位，`邮件-已读`本身是竖向打开信封且没有勾；两者必须分别按原始轮廓复刻，不能强制替换成普通 `邮件` 的横向外框。
- 人物增减等“主体 + 状态符号”图标必须分别测量主体末端与状态符号起点。以 `人员-加` 为例，肩线右端应按来源提前收口，与 `+` 的左端保留清晰空隙；不得为了填满外包范围延长肩线，导致两部分视觉粘连。
- `皇冠`、`VIP`虽然都有闭合外轮廓，但主体由尖角折线构成、内部留白大且实际墨量低；方形档位会明显偏小，应使用大型外包档位。闭合轮廓不是选择小档位的充分条件，还要判断内腔面积和线段分布。
- 人员状态图标必须复用同一人物头部与肩线基型，状态符号按各自来源位置独立绘制；`人员-加`、`人员-删除`、`人员-减`不能因为符号不同而改变人物比例或重新拉长肩线。
- `用户`及人员增、删、减状态图标在完整大型档位下会略显扩张，统一保留大型档位并使用 `0.94` 光学校正。人员状态三枚图标的主体基型与校正比例必须一致；状态符号只参与各自整体包围盒，不得单独拉伸人物主体。
- 消息类图标要先还原气泡数量、尾部方向和附加状态位置。`消息-添加`的加号位于气泡右下侧，`话题`是气泡内的井号，`评论`还包含右上方的第二层框线与两枚圆点；不得用常见同名符号替代。
- `密码箱-打开`是透视打开的箱门结构，`钥匙`的圆环在左下、齿杆指向右上。方向、遮挡和开合关系属于核心语义，不能只保留同类物体的通用轮廓。
- `钥匙`虽然是稀疏斜向轮廓，但长杆与齿部已经形成很强的方向张力；大型档位会显得过度扩张，应使用方形档位。斜向外接框较大时不能仅按低墨量放大，还要检查长轴的视觉冲击。
- `消息-添加`、`评论`的气泡边框都有让位或叠层开口，完整外接框并未形成同等视觉重量；加号、圆点和右上框线也属于轻量附属结构。方形档位会显小，应使用大型外包档位。不能把附属结构占到的几何范围误算为有效主体墨量。
- 共用同一气泡主体的状态变体应使用相同外包档位，但附加符号会改变整枚 SVG 的包围盒。`聊天记录`与`消息-添加`虽复用同一气泡路径，时钟和加号的横向占位不同，统一归一化后主体仍可能忽大忽小；验收时必须直接比较最终渲染的气泡外轮廓。
- `聊天记录`、`消息-添加`在方形档位偏小、完整大型档位又略大，最终都使用大型档位。为抵消不同附加符号造成的包围盒差异，当前分别使用 `0.892` 与 `0.94` 光学校正，使最终气泡外轮廓等大。图标族应统一主体的最终视觉尺寸，不要求校正数值机械一致。
- 名称不能替代来源结构：本项目的 `计算` 是“左上加号 + 对角分隔线 + 右下等号”，不是计算器；`微信支付`是带尾部的会话外轮廓与一条接入右上轮廓的支付折线，不是带两枚圆点的普通消息气泡。
- 购物车、钱包等复合物件必须保留来源的连接关系。购物车的把手、竖杆、车斗底边与斜边组成连续骨架，两枚车轮是独立点状结构；钱包的上沿、背部卡片与左侧开合曲线相互交接，不能改画成外置方形卡扣。
- 条形码的条数、横向节奏和上下错位共同构成来源特征。重绘时必须逐条核对，不能为了均匀或简化而删减条数、平均间距或拉齐所有端点。
- 同一来源的盾牌状态图标必须复用原始盾牌骨架，再分别还原内部语义。`盾-安全`是勾、`盾-危害`是叉、`盾-疑问`是问号、`盾-警告`是叹号、`盾-财产安全`是人民币符号；不得用同一个勾号草稿覆盖不同名称。
- 盾牌家族虽然有完整闭合外轮廓，但上宽下尖、内腔面积大，方形档位下有效主体明显偏小；完整大型档位又会略显扩张。已处理的 `盾-危害`、`盾-疑问`、`盾-安全`、`盾-警告`、`盾-财产安全`统一使用大型外包档位与 `0.94` 光学校正，使尺寸落在两档之间；内部状态符号不得单独改变比例。
- `票`的左右凹口、`插卡`的上下容器与插入卡片、`支付码`的四根竖条都属于来源拓扑。不能分别替换成常见票券圆孔、普通银行卡或二维码方块。
- `密码箱-关闭`中央是圆形旋钮与向下延伸的短杆，不是单独的感叹号或普通竖线；圆环、短杆、箱体和支脚必须作为同一个路径对象内的语义子路径保留。
- 同名金融与状态图标必须以来源内部语义为准：`盾-提示`为盾牌内的信息符号，`金币`为单枚圆币内的人民币符号，`自选`为书签加号，不能分别替换成加号盾、叠放硬币或五角星。
- `理财产品`的手提袋、上方提手与袋内趋势折线，`金融日期`的日历开口与右下折线，`黄金`的上一下二三块金条布局，都是来源的主体拓扑；不得用房屋、日期格、单块金条等常见同名图形简化。
- `图片`中的圆点细节应作为随描边变化的点状子路径处理，不能用写死长度的极短横线模拟；`WiFi`应按完整横向弧组判断外包，不能让最下方点状结构主导缩放。
- `认证`虽然是闭合徽章轮廓，但外缘由多段凹凸折线组成，实际主体被分散且内部留白较大；方形档位会显小，应使用 `28×28` 大型外包档位。不能因其外接框接近方形就机械归入方形档。
- 设备类图标必须按来源测量内部结构：`相机`镜头圆环的中心与半径、`路由器`指示点的位置、`视频通话`内点、`USB`顶部触点均不能凭常见图标印象放置。点状结构统一使用随描边变化的点路径。
- `录音`的有效主体包含话筒胶囊、下方接收弧和中轴延伸线；虽然整体纵向，但线条稀疏且内腔较大，应使用大型外包档位。`相机`与`视频通话`由横向机身决定有效主体，使用横向档位；`USB`由纵向插头主体决定有效主体，使用纵向档位。
- `图片-加载失败`必须保留来源中为右下叉号让位的断开画框、山形线和圆点；不能先画完整图片框再把叉号叠在外侧。`WIFI-未连接`的断开弧段与斜杠是同一来源构图，不得把完整 WiFi 图标与斜杠简单重叠后留下穿插杂线。
- `图片-加载失败`和`路由器`虽然几何外包接近方形，但前者由断开画框与轻量叉号组成，后者的大面积空腔只由细长天线和底座围合；两者在 `24×24` 方形档位下偏小、完整 `28×28` 大型档位又偏大，因此保留大型档位并使用 `26/28` 光学校正，使中心线路径外包约为 `26px`。
- 同一设备家族应复用已经按来源确认的基础骨架：`相机-禁用`沿用相机机身和镜头比例，仅在斜杠穿过处断开；`图片-添加`沿用图片画框、山形线和圆点，仅为右下加号让位。禁止重新画一套近似主体。
- `音量-关闭`、`音量-大`、`音量-小`必须共用来源扬声器基型；关闭叉、单层声波和双层声波只作为状态结构变化，不能导致扬声器主体宽度、折角或喇叭开口比例漂移。
- `收音机`的天线终点、两条短横线和旋钮圆环，`设备`的前后框层级及内部短线，均是辨识结构。内部圆环半径和短线长度必须从来源轮廓中线测量，不能按图标外框等比猜测。
- 带禁用斜杠的复合图标应按来源控制斜杠起止点，并在主体相交处保留正确遮挡或断开。斜杠不是默认贯穿整个 Keyline 的装饰线，不能机械使用画布对角线。
- 禁用斜杠的长度必须独立于整枚图标的外包范围校准。`录音-关闭`的斜杠若两端伸出主体过多，应保持中心与角度不变、同时收进两端；不能为配合过长斜杠而拉长右侧接收弧或改变话筒主体。
- `电话`的弧形听筒轮廓连续、方向张力强，完整 `28×28` 大型档位会显得扩张；保留大型档位的稀疏轮廓逻辑并使用 `26/28` 光学校正，使中心线路径外包约为 `26px`。
- `音量-关闭`由扬声器和独立关闭叉组成，分件间空隙使有效墨量进一步分散；`26×22` 横向档位会显小，应使用 `28×28` 大型档位。复合图标不能只凭整体长宽比选择横向档，还要判断分件密度。
- 数据图表类图标必须保留来源的分段关系：`最小值`与`平均值`的曲线会在虚线横带处断开，短横线的数量、长度和节奏不能改成连续曲线或平均分布；`折线图`、`数据显示`的折点位置也应从来源中心线测量。
- `智能AI`中的 `A` 与 `I`是主体语义，必须作为独立子路径完整保留；`数据-错误`的数据库层级与右下叉号应分别按来源定位，不能为了填满外包把叉号推到画布边缘。
- `代码`的左右括号不是普通大号尖括号：来源括号较窄、垂直跨度较短，中间斜杠更长且更陡。三部分必须按共同中心对齐，不能用常见代码图标比例替换。
- 带纸张开口的打印机外壳应拆成左右连续子路径，但每一段自身必须保持完整：左段从底部接口连续回到顶部起点，右段从顶部连续到右下接口。不能在子路径中误用闭合命令，把左侧竖边闭合成对角线或直接丢失。
- 数据库状态图标要先核对来源主体宽度，不能为了“同族统一”强制共用一套外壳。`数据-节点`的圆柱主体比 `数据-错误`、`数据-切换`、`数据-下载`、`数据库`、`数据-锁定`更窄；后五枚可共用宽圆柱比例，但右下状态结构需要各自保留来源中的让位和断口。
- `数据-节点`底部是与中轴相连的空心圆节点，左右基线在圆环两侧断开；不能替换成实心点，也不能让基线穿过圆环。`数据-切换`的两支箭头方向相反，`数据-下载`的箭杆与两翼以同一尖点连接，状态结构的方向和连接点都必须按来源还原。
- 图标名称不能决定通用隐喻：本项目的 `精确准确`是带四向伸出线的方形准星，`快速高效`是纵向推进器结构，`开放`是三枚空心圆节点及三段连接弧，`灵活扩展`是三条分支连接三个方框，`流转`是上下两条矩形回转箭头。禁止用靶心、秒表、开锁、四宫格或圆形循环箭头替代。
- `开放`的三枚节点圆环必须同径，三段连接线必须落在同一个圆心和半径上，只连接节点之间的空段，不得穿过圆环。完整大型档位会显大，而 `0.90` 又略显小，当前使用大型档位与 `0.94` 光学校正；缩放不能破坏外环的正圆关系。`灵活扩展`的分支线必须真实接到各方框边缘；其三条细长开放支线与小端框导致有效墨量低，当前使用方形档位与 `1.06` 整体光学校正。`流转`的两条箭头保持上下平行、方向相反，并保留来源中的直角折返比例。
- `快速高效`是开放式推进器骨架，不是底部封口的普通上箭头。左右主体边从尖顶向下延伸，侧翼分别接入两边，中间推进线独立下伸；禁止用底部横线把两侧主体闭合成房屋或箭头轮廓。其主体窄、内部开放，当前使用方形档位与 `1.06` 整体光学校正。
- `流转`由两条开口较大的矩形回转箭头组成，内部负空间和断口会削弱有效墨量；`24×24` 方形档位会显小，应使用 `28×28` 大型外包档位。放大时不得改变两条箭头的等距关系、相反方向和直角折返比例。
- `查验`由三枚分离的取景框角和右下放大镜组成，断口与圆形镜片使有效墨量低于同外接框的闭合图标。当前整体中心线外包由约 `21px` 放大至 `24px`；三处直角、镜片与手柄必须同步等比移动，描边宽度保持用户设定值。
- `接口配置`是三条横向轨道与三枚空心圆形滑块，滑块分别位于左、右、左；不能用短竖线替代圆形滑块。轨道必须在圆环两侧断开，不能穿过圆环。
- 名称为 `管理` 的来源图标是齿轮，不是三节点组织关系图。齿轮外圈的凹凸节奏与内圆必须同时还原；同名业务含义不能覆盖来源图形。
- `证照执照`的来源结构是横向证照框、中央五角星和底部横线，不是多行文字加圆形印章。`印章认证`则包含上方握柄、两侧收束的印章底座和独立底线，两者不得互换常见隐喻。
- `文档`必须保留右上折角和三条来源横线，其中第一条较短且位于折角左侧；不得把横线平均拉齐或额外增加第四条。`应用场景`的四个方框同尺寸、同间距，必须按来源的小方框比例重建，不能为了填满外包擅自放大每格。
- 业务盾牌应优先使用各自来源骨架。`安全隐私`的盾体比已确认的 54209 盾牌家族更窄，勾号位置也不同；不得仅因语义相同而复用其他合集的通用盾牌组件。
- `精确准确`虽有方形外框，但四向短线、内部大留白和分散交点使有效墨量偏低；`24×24` 方形档位会显小，应使用 `28×28` 大型外包档位，并以 `1.05` 做轻量整体光学校正。放大时只调整整枚图标外包，不改变准星方框、伸出线和中心交点的相对比例。
- 图标名称只能用于定位来源，不能用于选择通用隐喻。本合集的 `公益`是心形轮廓，`隐私保护`是锁体而不是盾牌；即使项目中已经有相近语义的已确认图标，也必须重新核对当前 source id 的实际拓扑。
- 身份类变体必须保留各自容器：`身份认证`是横向证件框，左侧为头像、右侧为两条信息线；`身份识别认证`是四枚分离取景角包围头像。不得为了家族统一互换外框，也不能让取景角连接成完整方框。
- 建筑和信封的内部线不是可省略装饰。`政府`必须保留来源的三开间节奏（四根竖向边界）和完整底座；`邮件邮箱`除上方开合 V 形外，还要保留下方两条折叠斜线，不能简化成普通邮件图标。
- `发票`与`订单`都含矩形主体，但拓扑不同：发票有后层矩形、前层票据及锯齿底边；订单是带顶部夹具的剪贴板和两条内容线。重绘时应分别核对遮挡、层级和开口，不能套用同一个文档骨架。
- `红包`的来源外廓本身是约 `4:5` 的竖向矩形，必须使用 `22×26` 纵向 Keyline，不得套用方形或圆形档位，也不得为了填满宽度而拉伸变形。按来源比例等比适配后中心线外包约为 `20.8×26px`，并保持信封翻盖结构。
- `仓储仓库`的屋顶、侧墙和内部两条货架线共同决定来源比例；内部货架线较短且水平居中，不得拉到侧墙，也不能用普通房屋门窗替代。
- `安全隐私`的盾牌顶点必须是盾体左右上边的真实汇合点。闭合路径进入圆角处理后若把首尾节点重复解释，会在顶部形成叶片状回折；此类带固有曲线的盾体应使用明确回到顶点的连续线段闭合，并保持下半部来源曲线不受圆角参数改形。
- `仓储仓库`与`发票`虽然外接框接近方形，但前者由开放屋顶和短货架组成，后者由轻量后框、窄票据与锯齿底边组成，`24×24` 方形档位下有效主体均偏小。两者使用大型档位与 `26/28` 光学校正，使最终中心线外包约为 `26px`，内部结构与描边不随之单独变化。
- 商业图标不得按常见图库含义替换来源结构：`卖家购物车`没有篮筐上方提手，`卖家店铺`没有门窗，`票据`是带左右凹口和纵向分隔线的横向票券，`全领域规模`是四个高低交错的矩形模块而不是地球。
- 同一店铺语义必须沿用已确认的店铺视觉语言：`卖家店铺`使用水平顶边、左右斜边、三段等节奏圆弧檐口和下方 U 形店体。不得把檐口改成自由波浪线，也不得在无来源依据时加入门窗。
- 货币图标的圆形外圈属于主体，不是可省略容器。`欧元`和`美元`必须同时保留同径圆环与内部币种符号；内部符号按圆心单独校准，不能为了扩大字符而侵占圆环留白。
- `工厂`顶部飘带、阶梯式厂房和四个方形窗点均为来源结构；窗点的宽高随描边宽度变化，不得用固定极短线模拟。`合同`右下认证章必须包含完整圆环和内部星标，不能只把五角星贴在普通文档外侧。
- `合同`由文档和右下认证章组成，认证章横向突出但两部分均为细线、内部留白较大；纵向 `22×26` 档位会让完整组合显小，应使用 `28×28` 大型档位。放大时文档、认证章和星标作为一枚图标同步缩放，不能单独放大认证章。
- `海关港口`的中轴、顶部锚环、横杆、两侧钩臂与底部延伸线构成一个整体；不得把顶部结构改成封闭叶片或把两侧箭形钩臂简化成普通圆弧。
- 同一货币系列必须共享相同圆形外圈基型。`人民币`与已完成的欧元、美元一样，圆环和内部币种符号缺一不可；人民币符号的两条横线、竖线和顶部两条斜线必须保持来源连接关系。
- `扫码`是四枚取景角加一条横向扫描线，不是二维码方块；`融资资金`是圆环内的空心菱形，不是数据库或硬币堆。名称中的“码”“资金”不能覆盖 source id 的具体图形。
- 全球类图标需要区分结构：`全球跨境`是球体与斜向环绕轨道，`商业化全球`是圆形地球与内部大陆线；不得统一改成经纬线地球，也不能用外置箭头或货币符号替代来源内部结构。
- `全球跨境`的完整环绕轨道扩大了几何包围盒，但视觉主体仍由稀疏球体和细环构成；标准大型档位下仍显小，使用 `1.08` 光学校正。校正只放大整枚图标，不改变球体与轨道的比例、角度或描边宽度。
- `物流`必须读作完整货车：左侧驾驶室、右侧货厢、车底连接线和两枚同径车轮缺一不可。不能只保留驾驶室折角和货厢内横线；货厢与驾驶室要真实接合，车轮不得被外包归一化裁掉。
- 货车车身底线必须在车轮内部隐藏，但轮外两侧仍要连续接到圆周。断点应精确落在底线与车轮圆周的两个交点上，不能留下可见空隙，也不得让横线穿过轮内形成多余线段和加粗交点。
- 医疗类图标也不能共用通用医疗箱：`医疗服务`是带顶部提手的医疗箱和中央十字，`医疗机构`是高低两栋建筑、门洞、十字及右侧短窗线。主体层级必须分别还原。
- `底层架构`由顶部闭合菱形层和下方两条开放叠层线组成。三层共享同一中心轴与倾斜角，不能画成三个完整封闭菱形，也不能让下层左右端点不齐。
- 数据业务图标必须先核对基本拓扑：`饼图图表`是完整圆环加三条扇区分隔线，`分布式`是上方一枚、下方两枚的三个六边形，`存储`是软盘而不是数据库圆柱，`分支`是三节点层级树而不是弯曲 Git 线。
- `服务器`的三层机柜共享一个外框，两条水平分隔线贯穿，三条短状态线位于每层右侧；不得改成三个分离盒子或把状态点放到左侧。`技术服务`则是显示器、左右代码括号和独立底座线，不能与服务器混用。
- `丰富多元`的两个方形端点、折线路径及上下延伸线共同构成来源结构；`联盟链`是从中心向上、左下、右下延伸的三向尖头分叉，不是两个链环，也不是三根普通平头 Y 形线。三枚尖头必须分别保留来源的窄长比例并与各自支线连接。
- `监管风控`复用当前 source id 的盾牌比例，内部按产品确认使用水平、垂直等长交叉的居中十字架；不得恢复为斜线、勾号、叹号或省略号。盾体顶点仍按明确闭合规则处理，避免产生叶片状回折。
- 基础设施与平台图标必须保留来源的具体结构：`区块`是三面共用中心接点的立方体；`平台`是带两条内容横线和独立底座的显示器；`上云`是云体、菱形上箭头及下方箭杆的组合，不能替换成普通上传箭头。
- `区块`的六边形外廓较窄且内部三分线带来大量留白，方形档位会使有效主体偏小；使用大型档位与 `0.9286` 光学校正，使纵向中心线外包约为 `26px`。立方体外廓、中心接点和三条分面线必须整体同步缩放。
- `上云`的云体内部留白较大，横向外包不能代表有效视觉重量；在横向档位上使用 `1.08` 整体光学校正，云体与内部箭头同步放大，描边宽度保持独立。
- 时间类名称不能决定惯用图形：本项目的 `时间周期`是上下横杆、两侧折线和中央收束点组成的沙漏，`时间戳`才是圆形时钟；不得用历史记录环形箭头或秒表互相替代。
- `时间周期`的纵向沙漏轮廓连续且上下横杆形成较强视觉重量，使用纵向档位的 `0.90` 光学校正；沙漏各部分必须整体缩放，不能单独缩短横杆或压缩内部间距。
- `眼睛-不可见`按产品最终确认使用初始版本的完整横向扁平眼廓和完整正圆瞳孔；独立对角斜杠保持原角度，但长度为初始版本的 `90%`。该图标不采用断开圆弧方案，也不得把外眼廓改成同心圆。
- 数据图形必须按来源拓扑区分：`数字化`是大小两部分分离的饼图，`图表柱图`是三枚高低不同的独立空心柱；不能改成四宫格、带坐标轴的实心柱图或折线图。
- `数字化`虽然属于圆形结构，但闭合面积与直角分隔形成较强视觉重量；使用圆形档位的 `0.90` 光学校正，两个饼图分件必须同步缩放并保持原有间隙。
- `溯源`是同心双圆与一条从共同圆心延伸至右下外圈的斜向连杆，不能画成时钟、环形箭头或路线节点；两个圆必须同心，连杆与内外圆真实相接。
- `稳定可靠`是上方一枚、下方两枚的三个同径空心圆节点及三条三角连接边，不能画成吊坠、实心三角或盾牌勾号；连接边在圆周处收口，圆内不得露出多余线段。`网络`是圆形外框、赤道和两组对称经线组成的地球，不是三节点连接图。
- 最后一组仍以 source ID 为唯一结构依据：`音乐`是斜向横梁连接的双音符，左右音符头高度不同；`性能计算`是四边各三枚引脚、双层方框的芯片，不是芯片内时钟或折线。
- `数据监管`是显示器外框、三根高低不同的柱线和独立底座，不是数据库叠层加盾牌；`远程穿透`是上下两个左开口框架共用中轴，并由一条水平右箭头贯穿，不是两个完整方框和循环箭头。
- `智能合约`是横向面板、左下右向指令折角、顶部通栏与右侧两条短内容线；不得替换成折角文档、代码尖括号或斜杠。
- `音乐`、`性能计算`、`数据监管`、`远程穿透`均使用 `1.10` 整体光学校正。原因不是几何边界不足，而是有效主体被大留白、细长音符杆、离散引脚、短柱线和开放框架分散；名义外接框包含大量低墨量区域，按边界直接比较会系统性显小。此类图标应以连续主干、封闭主体面积和单位面积墨量为准，所有组成部分同步放大，禁止只拉长引脚、箭头或底座。

### 2.6 尺寸校准顺序

1. 还原来源中心线和正确拓扑。
2. 在原始轮廓叠加图中确认节点、开口、内部间距和各分件比例。
3. 判断有效主体、墨量、负空间和方向性，选择初始外包档位。
4. 在网页以 `32px / 2px 描边 / R1` 查看，并与当前批次及已确认样板交叉比较视觉重量。
5. 先判断问题类别：整体尺寸、内部间距、附属节点干扰或重心偏移；只修改对应层级，禁止用整体缩放处理所有问题。
6. 档位过大或过小时先切换档位；两档之间仍不平衡时，再使用 `0.92–1.08` 的单图标光学校正。
7. 用户反馈后必须把误判原因回写到本规范的案例和判断规则中，不能只修改当前图标。
8. 最后检查 `16/24/32` 尺寸、`1/2/4/10` 描边和 `R0/R1/R2`，确认外边界、内部负空间、端点和连接关系稳定。

## 3. 路径拓扑

- 每个图标只输出一个 SVG `<path>` 元素，避免在 MasterGo 中拆成多个矢量图层。
- 一个 `<path>` 内允许存在多个 `M` 子路径：例如箭头的三叉节点、信息点、互不相连的语义部件。这是一个矢量对象，不是多个图层。
- 能连续的轮廓必须连续，不得在普通转角或相交点切断。
- 来源中的文字、数字或字母若承担图标语义，必须以中心线路径重建，并与边框、箭头等结构共同保留在同一个 `<path>` 对象中；禁止因其结构复杂而删减字符。
- 环形箭头（如旋转、历史记录、刷新）必须把“圆环”和“箭头折角”作为两个结构处理。箭头须由连续的三点折线绘制，使中间尖角能够独立响应 `R0/R1/R2` 圆角参数；圆弧不得终止在这个尖角节点，而应连接到箭头的一侧尾端。两条箭头边须等长、夹角一致且都能清楚识别，不能让其中一边与圆弧重叠成单边箭头。圆弧与箭头尾端的连接必须真实闭合，不能用悬空、近似靠近或额外短线伪造。
- 禁止直接使用自动中轴算法的分支结果。自动算法只能作为定位参考，最终节点和拓扑必须人工确认。

## 4. 圆角与端点

- 圆角参数只修改中心线真实几何拐点，不修改曲线语义、圆形、盾牌弧线等固有轮廓。
- 圆角值 0/1/2 必须分别可见且可预测；默认 1。
- 默认端点为直线端点（butt），默认边角为直角（miter）。需要圆弧的地方用路径几何显式构造，不能依赖全局 round cap/join 伪造。
- 点状细节的宽高必须与当前描边宽度一致，并合并在同一个 `<path>` 对象中。
- 警示光线等“短线点”若以描边宽度定义比例，必须随描边参数动态生成，不能写死坐标长度。`警报`顶部及两侧三条短线的宽度等于当前设定线宽，中心线长度等于线宽的 `2` 倍；三条线保持各自方向和中心位置，并继续合并在同一个 `<path>` 对象中。
- `警报`的三条警示短线扩大了完整几何包围盒，但有效主体仍是内部留白较大的单层拱形灯罩；方形档位会让灯罩主体明显偏小，应使用大型外包档位。不能让轻量短线主导整枚图标的尺寸判断。

## 5. 逐图标验收

每个图标替换前必须同时通过：

1. 原图叠加对照：构图、比例、开口、节点和视觉重心一致。
2. 网格检查：32 网格内使用合适的 `24×24 / 28×28 / 22×26 / 26×22` 外包档位；同时检查有效主体、墨量、内部负空间、极端附属节点和视觉重心，并与当前批次及已确认样板比较，不能只凭几何包围盒判定合格。
3. 参数检查：尺寸 16/24/32、描边 1/2/4/10、圆角 0/1/2 无断裂、毛刺或错连。
4. DOM 检查：图标主体只有一个 `<path>` 元素，无空路径和无效坐标。
5. MasterGo 检查：复制后为一个矢量对象，节点可编辑，端点与边角类型正确；在 `32px` 画布下方形路径为 `24×24`、圆形路径为 `28×28`、左右分隔箭头为 `10×18`、关闭图标为 `18.5×18.5`、变大/变小为 `22×22`，其他画布按对应档位和原始长宽比计算，描边宽度与用户设置一致。
6. Iconfont 检查：导出后为闭合填充轮廓、`fill-rule="nonzero"`，可正常上传。

> MasterGo 说明：普通 SVG 只能记录最终曲线，不能记录 MasterGo 的非破坏性“节点圆角值”。因此 MasterGo 可编辑副本必须输出未烘焙圆角的折点中心线；Iconfont 副本才输出已经轮廓化的当前圆角效果。复制到 MasterGo 前还必须把网页中的 viewBox、Keyline 和光学缩放变换烘焙进路径坐标，把复制 SVG 的 viewBox 归一为用户选择的像素尺寸，并把最终物理描边宽度写回路径。MasterGo 属性面板中的路径宽高必须按中心线 Keyline 显示，描边宽度必须与用户设置一致；不得预减描边，也不得把内部补偿值直接交给 MasterGo。两条导出链路不可混用。

## 6. 样板图标

- 样板一：`下载`（合集 54209，source id 47901520）。箭头与托盘使用同一 `<path>` 的三个子路径；分叉节点连续，端点为 butt，转角为 miter，几何圆角只作用于箭头折点和托盘底角。
- 样板二：`主页`（合集 54209，source id 47901534）。房屋使用封闭中心线与独立门线；网页预览采用内描边轮廓，外侧 keyline 固定，描边只向内增长。MasterGo 的“内部描边”属于宿主软件属性，普通 SVG 不能可靠携带，后续由插件写入。
- 样板三：`查验`（合集 26815，source id 18267910），已于 2026-08-28 通过网页验收：
  - 按原始 1024 轮廓直接换算到 32 网格，没有替换为其他图标库的近似图形。
  - 三个取景框转角是可调几何圆角；放大镜圆弧属于图形固有曲线，不受圆角参数二次改形。
  - 三个框角、镜片与手柄全部位于同一个 `<path>` 中，以 `M` 子路径区分语义部件。
  - 圆角 0/1/2、描边和尺寸变化均保持端点完整，无自动中轴断点。
- 批量更新顺序为“基础功能 → 业务类 → 生活服务 → 金融数据类”。每一枚图标必须标记为“结构合格”和“视觉复刻合格”后才算完成；仅通过自动检查不能替代原图对照。

## 7. 批量复刻禁止项

- 禁止把旧版 `flo-mat` 中轴分支直接作为成品路径；旧生成文件仅用于查找大致中心位置。
- 禁止用增加 `round` 端点、扩大描边或重叠遮盖的方式掩盖断点。
- 禁止为了凑成一个 DOM 元素而改变原图拓扑；一个 `<path>` 可以包含多个正确的 `M` 子路径。
- 禁止在未对照原始 SVG 的情况下声称图标已经 1:1 完成。

## 8. AI 新生成图标的强制规范

AI 生成与人工重绘共用本文件的来源优先、中心线、拓扑、视觉体量和单路径原则。代码单一来源是 `src/icon-generation-standard.ts`，当前版本为 `FS-LINE-2026.09`；修改本节时必须同步修改该文件，反之亦然。

### 8.1 24 画板映射

AI 输出保留 `0 0 24 24` viewBox，并把第 2 节的正式 32 画板中心线 Keyline 等比映射为：

| 类型 | 24 画板中心线路径范围 |
| --- | ---: |
| 方形 / 密集 | `18×18` |
| 圆形 / 大型 / 稀疏 | `21×21` |
| 纵向 | `16.5×19.5` |
| 横向 | `19.5×16.5` |
| 左右纯箭头 | `7.5×13.5` |
| 上下纯箭头 | `13.5×7.5` |
| 关闭 | `13.875×13.875` |
| 变大 / 变小 | `16.5×16.5` |

最小画板留白为 `0.75`。描边宽度和几何圆角由用户选择的显示尺寸换算到 viewBox，但始终是独立参数，不能通过扩大或缩小路径伪造。

### 8.2 Prompt 必须表达的规则

- 有参考图时，参考图是结构真值：物件数量、开口、重叠、方向、连接点、内部符号和长宽比必须保留；概念名不能覆盖参考结构。
- 无参考图时，只使用最简单且无歧义的线性拓扑，不添加装饰性结构。
- 选择 Keyline 时同时判断有效主体、墨量、负空间和方向性；不能仅凭名称或总外接框。
- 宽高必须等比适配，禁止分别拉伸。
- 连续来源线必须真实连续；不能用重叠、加粗或圆头隐藏缺线。
- 默认 butt 端点和 miter 转角；需要曲线或圆角时用 `A/Q/C` 明确构造。

### 8.3 模型输出合同与代码校验

- 只允许输出一个 `<path d="..." fill="none"/>`，不允许 `<svg>`、`<g>`、基础图元、文字、样式或说明。
- 不相连部件必须放在同一个 `d` 内，以多个绝对 `M` 子路径表达。
- 只允许绝对命令 `M L H V C S Q T A Z`，禁止相对坐标和 `transform`。
- 禁止 `stroke`、`stroke-width`、`stroke-linecap`、`stroke-linejoin`、`class`、`id`、`opacity` 等额外属性。
- 结果必须能解析为有效几何，且实际路径边界完全位于 `24×24` 画板内。
- 通过新版校验后直接使用模型输出的规范坐标，禁止再套用旧的自动光学缩放，否则会把已按圆形或特殊 Keyline 绘制的结果二次改小。
- 新生成结果不满足任一项就直接拒绝，不能自动放宽规范。旧云端自定义图标只经过兼容性的安全解析，避免历史数据因规范升级而消失。
